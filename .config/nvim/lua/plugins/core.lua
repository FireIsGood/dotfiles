return {
  "LazyVim/LazyVim",
  opts = {
    colorscheme = "catppuccin-macchiato",
    defaults = {
      keymaps = false,
    },
  },
  checker = {
    -- enabled = true,
    notify = false,
    -- frequency = 86400, -- check for updates once a day
  },
}
